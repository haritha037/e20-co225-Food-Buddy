import 'package:get/get.dart';

class Dimentions {
  static double screenHeight = Get.context!.height;
  static double screenWidth = Get.context!.width;

  static double hUnit = screenHeight * 1 / 890;
  static double wUnit = screenWidth * 1 / 411;
}
